import { RouterModule, Routes } from '@angular/router';
import { GenrealComponent } from './genreal/genreal.component';
const SourceRoutes: Routes = [

  // { path: '',
  // runGuardsAndResolvers: 'always',
  // children: [
  //     { path: 'general', component: GenrealComponent },
  // ] }


];
